// Local feature flags for the prototype. Replace with RevenueCat / real IAP
// when premium gating goes live (see PROTOTYPE_INTEGRATION_PLAN.md §1).

export const FeatureFlags = {
  // Set to true to act as a Premium user (unlocks Mad Libs builder, etc.)
  isPremium: true,

  // Set to true to act as a Professional user (unlocks therapeutic features)
  isProfessional: false,
};
